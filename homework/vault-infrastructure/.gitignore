# Vault sensitive files - NEVER commit these
scripts/vault-keys.json
scripts/approle-credentials.txt

# TLS Certificates (regenerate for each deployment)
certs/*.pem
certs/*.key
certs/*.crt
certs/*.csr

# Logs
logs/*.log
logs/*.json

# Temporary files
*.tmp
*.bak
.DS_Store
